services:
  rabbitmq:
    image: rabbitmq:3-management
    container_name: bytedeals-rabbitmq
    ports:
      - "5672:5672"
      - "${RABBITMQ_UI_PORT}:15672"
    environment:
      - RABBITMQ_DEFAULT_USER=${RABBITMQ_USER}
      - RABBITMQ_DEFAULT_PASS=${RABBITMQ_PASS}
    env_file: 
      - .env
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 20s
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq

  prometheus:
    image: prom/prometheus
    container_name: bytedeals-prometheus
    ports:
      - "${PROMETHEUS_PORT}:9090"
    volumes:
      - ./infra/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    env_file: 
      - .env

  grafana:
    image: grafana/grafana
    container_name: bytedeals-grafana
    ports:
      - "${GRAFANA_PORT}:3000"
    env_file: 
      - .env
    volumes:
      - grafana-data:/var/lib/grafana
      - ./infra/grafana/provisioning:/etc/grafana/provisioning
      - ./infra/grafana/dashboards:/var/lib/grafana/dashboards

  loki:
    image: grafana/loki:latest
    container_name: bytedeals-loki
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    volumes:
      - ./infra/loki/config.yml:/etc/loki/config.yml
      - loki-data:/loki
  tempo:
    image: grafana/tempo:latest
    container_name: bytedeals-tempo
    command:
      - "-config.file=/etc/tempo.yaml"
    volumes:
      - ./infra/tempo/tempo.yaml:/etc/tempo.yaml
    ports:
      - "3200:3200"
      - "4317:4317"
    restart: unless-stopped

  rabbitmq-exporter:
    image: kbudde/rabbitmq-exporter:latest
    container_name: bytedeals-rabbitmq-exporter
    environment:
      RABBIT_URL: http://rabbitmq:15672
      RABBIT_USER: ${RABBITMQ_USER}
      RABBIT_PASSWORD: ${RABBITMQ_PASS}
    ports:
      - "9419:9419"
    depends_on:
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: bytedeals-cadvisor
    ports:
      - "8081:8080"
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker:/var/lib/docker:ro
    privileged: true
    restart: unless-stopped

  node-exporter:
    image: prom/node-exporter:latest
    container_name: bytedeals-node-exporter
    ports:
      - "9100:9100"
    restart: unless-stopped

  promtail:
    image: grafana/promtail:latest
    container_name: bytedeals-promtail
    volumes:
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - ./infra/promtail/config.yml:/etc/promtail/config.yml
    command: -config.file=/etc/promtail/config.yml
    depends_on:
      - loki

  ollama:
    image: ollama/ollama
    container_name: bytedeals-ollama
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - OLLAMA_KEEP_ALIVE=24h
    volumes:
     - ollama_data:/root/.ollama
    restart: always

  ollama-init:
    image: curlimages/curl
    container_name: bytedeals-ollama-init
    depends_on:
      - ollama
    entrypoint: >
      sh -c "
        echo 'Esperando Ollama...';
        
        until curl -s http://ollama:11434/api/tags;
        do
          sleep 2;
        done;

        echo 'Baixando modelo...';

        curl http://ollama:11434/api/pull -d '{
          \"name\": \"qwen2.5:1.5b\"
        }';

        echo 'Aquecendo modelo...';

        curl http://ollama:11434/api/generate -d '{
          \"model\": \"qwen2.5:1.5b\",
          \"prompt\": \"oi\",
          \"stream\": false
        }';

        echo 'Modelo pronto!';
      "

  bot:
    build:
      context: .
      dockerfile: bot/Dockerfile
    container_name: bytedeals-bot
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    env_file: 
      - .env

  extractor:
    build:
      context: .
      dockerfile: extractor/Dockerfile
    container_name: bytedeals-extractor
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    env_file: 
      - .env

  publisher:
    build:
      context: .
      dockerfile: publisher/Dockerfile
    container_name: bytedeals-publisher
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
    env_file: 
      - .env  

  agentia:
    build:
      context: .
      dockerfile: agentIA/Dockerfile
    container_name: bytedeals-agentia
    restart: always
    depends_on:
      rabbitmq:
        condition: service_healthy
      ollama:
        condition: service_started
      ollama-init:
        condition: service_completed_successfully
    env_file: 
      - .env 

volumes:
  rabbitmq-data:
  prometheus-data:
  grafana-data:
  loki-data:
  ollama_data:

